<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  </head>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Archivo+Black&family=Montserrat:wght@700&display=swap');

    table{
      justify-content: center;
      margin-top: 3rem;
    }

    .service{
      text-align: left;
      margin: auto 50rem;
    }

    .table-name{
      font-family: 'Archivo Black', sans-serif;
      text-align: center;
    }

    .btn{
      font-family: 'Montserrat', sans-serif;
      width: 10rem;
      margin-bottom: 0.5rem;
      text-align: center;
    }
  </style>
  <body>
    <div class="service show">
      <h2 class="table-name">Show Service</h2>
      <table class="table table-striped" summary="Service detail view">
        <tr>
          <th>Hours</th>
          <td><%= @service.hours %></td> 
        </tr>
        <tr>
          <th>Description</th>
          <td><%= @service.description %></td>
        </tr>
        <tr>
          <th>Date</th>
          <td><%= @service.format_date %></td>
        </tr>
        <tr>
          <th>Approved?</th>
          <td>
            <% if @service.isApproved %>
              <%= image_tag @service.approved_img_path, height: 30 %>
            <% else %>
              <%= image_tag @service.approved_img_path, height: 30 %>
            <% end %>
          </td>
        </tr>
      </table>
      <% if !current_member.isAdmin && !@service.isApproved %>
        <div class="col-2"><%= link_to("EDIT", edit_service_path(@service), :class => 'action edit') %></div>
        <div class="col-2"><%= link_to("DELETE", delete_service_path(@service), :class => 'action delete') %></div>
      <% end %>
      <%= link_to("BACK", services_path, :class => 'back-link btn btn-primary') %>
    </div>
  </body>
</html>

    
